{
  "name": "@typing-race",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:be": "npm run start --workspace=@typing-race/be",
    "start:fe": "npm run start --workspace=@typing-race/fe",
    "prepare-deploy": "npm run prepare-deploy --workspace=@typing-race/fe",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "es-toolkit": "^1.38.0"
  }
}
